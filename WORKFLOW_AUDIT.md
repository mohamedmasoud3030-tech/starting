# حصر المزايا وتصحيح الوورك فلو — مدير منتج + خبير تشغيل ضيافة

> آخر مراجعة: 2026-09-13 (النسخة 2 — مُراجَعة ومُصحَّحة بعد معاينة بصرية حقيقية على الموبايل وسطح المكتب).
> **الوضع:** التطبيق لم يدخل المكتب بعد؛ كل البيانات تجريبية. الوثيقة تصحّح ما بُني، لا تضيف جديداً.

---

## 0. أخطاء النسخة الأولى من هذه الوثيقة (وما تعلّمناه)

| الخطأ | لماذا حدث | التصحيح |
|---|---|---|
| **لم تكتشف تكرار «المعدات» و«المخزن»** — تبويبان فوق جدول واحد (`event_equipment_reservations`) | الحصر كان بأسماء الشاشات لا بالجداول | القاعدة الجديدة: **الحصر يبدأ من الجداول** (القسم 1-ب). دُمجا في تبويب واحد «المخزن» |
| ادّعت أن «زر أرسل للعميل واتساب» و«زر إقفال يفعّل عند صفر» جزء من الخطة المنفَّذة | كُتبت كـ"ما يظهر أولاً" ولم تُنفَّذ | حُذفت من قائمة المنفَّذ؛ زر الواتساب للعميل غير موجود (القسم 6) |
| قالت «الشريط الأخضر WDAH ALANTALAQ من Vercel» | تخمين بلا معاينة | كان كوداً في `PageHeader`؛ أُزيل |
| اعتبرت `/consumables` «كما هو ✅» | لم تلاحظ أنه لا توجد صفحة على مستوى المكتب لعدة العرض (فناجين/دلال) رغم وجود جدول `equipment_capacity` وRPC `equipment_availability` | صفحة «المخزن» أصبحت تبويبين: **العدة | المواد** |
| لم تُختبر على الشاشة | فحص ثابت فقط | كل تغيير يُعاين الآن بمتصفح حقيقي (`/home/user/shots`) |

---

## 1. طريقة عمل المكتب (المرجع الوحيد)

```
① العميل يتصل: كم شخص؟ فين؟ إمتى؟
② العرض يتحدد من الجدول (6 عروض) → يُرسل للعميل
③ العميل يوافق → يدفع 30% عربون → الحجز مؤكد (قبل 72 ساعة)
④ إعلان في جروبات واتساب المضيفين → كل من يرد «تأكيد» يُسجَّل اسمه
⑤ المخزن يجهّز عدة العرض (طاولات، مفارش، فناجين، دلال، قرطاسية…)
⑥ يوم المناسبة: بصمة وجه عند التحرك → تنفيذ → بصمة وجه عند الانتهاء
⑦ بعدها: العميل يكمّل الباقي · المضيفون يقبضون · العدة ترجع ويُحسب التالف/المفقود · إقفال
```

قاعدة التصحيح: **كل شاشة تخدم مرحلة واحدة من السبعة، وإلا تُخفى. وكل جدول له شاشة واحدة، وإلا تُدمج.**

---

## 1-ب. الحصر من الجداول (المصدر الحقيقي للتكرار)

| مجال | الجداول | الشاشات التي تقرأ/تكتب فيها | الحكم |
|---|---|---|---|
| العميل | `customers` | `/customers` · المناسبة السريعة | ✅ واحد لواحد |
| العرض | `packages` `package_items` `quotations` `quick_quotes` | `/packages` · `/quotes` · التسعير داخل المناسبة | ⚠️ ثلاث واجهات لفكرة واحدة؛ مقبول مؤقتاً: `/quotes` للحالات الخاصة فقط |
| المناسبة | `events` `event_status_history` | `/events` · `/calendar` · `/operations` · اليوم | ⚠️ `/operations` يكرر اليوم — تحت «المزيد» حتى الدمج |
| **العدة (تُحجز وترجع)** | `equipment_capacity` `event_equipment_reservations` `event_equipment_movements` `event_warehouse_reconciliations` | كانت: «المعدات» + «المخزن» داخل المناسبة، ولا شيء على مستوى المكتب | ✅ **صُحِّح**: تبويب واحد «المخزن» داخل المناسبة + تبويب «العدة» في صفحة المخزن |
| **المواد (تُصرف وتخلص)** | `consumable_stock_items` `consumable_movements` `event_consumable_reconciliations` | «المواد» داخل المناسبة + `/consumables` | ✅ ليست تكراراً مع العدة (جداول وحسابات مختلفة) |
| الشراء | `suppliers` `procurement_orders` `..receipts` `supplier_invoices` `..payments` | `/procurement` · «المشتريات» داخل المناسبة | ✅ |
| المضيفون | `staff_members` `event_staff_assignments` `attendance_records` `payroll_*` `advances` | `/staff` · «الفريق» · «الحضور» · «الأجور» | ✅ |
| الفلوس | `payments` `invoices` `journal_entries` `chart_of_accounts` | «المدفوعات» · «الفواتير» · «المالية» · `/accounting` | ✅ (خطأ خادم في دوال الأعمار — migration 0104) |

---

## 2. حصر الشاشات (بعد التصحيح)

| # | الشاشة | المرحلة | الحالة الآن |
|---|---|---|---|
| 1 | `/home` اليوم | لوحة | ✅ أسئلة المالك الثلاثة · عمودان على الديسكتوب · 2×2 على الموبايل |
| 2 | `/events` | ①–⑦ | ✅ «مناسبة جديدة» = السريعة؛ «مناسبة مخصصة» للاستثناء |
| 3 | `/events/$id` | ③–⑦ | ✅ شريط مراحل مرقّم (ملخص · ١ العرض والعربون · ٢ المضيفون · ٣ العدة · ٤ يوم المناسبة · ٥ الإقفال · السجل) |
| 4 | `/quotes` | ② | تحت «المزيد» كـ«عروض أسعار مخصصة» |
| 5 | `/customers` | ① | ✅ هاتف واحد = واتساب |
| 6 | `/packages` | ② | «العروض الستة» تحت «المزيد» (تُزرع تلقائياً من المناسبة السريعة) |
| 7 | `/catalog` | ⑤ | «دليل العدة والمواد» تحت «المزيد» |
| 8 | `/consumables` | ⑤⑦ | ✅ **أصبحت «المخزن»: العدة \| المواد** |
| 9 | `/procurement` | ⑤ | «الموردون والشراء» تحت «المزيد» |
| 10 | `/procurement/restaurants` | ؟ | تحت «المزيد» — لم يذكرها المالك؛ مرشّحة للإخفاء |
| 11 | `/staff` | ④⑥⑦ | ✅ «المضيفون والحضور»، إضافة مضيف، لغة بسيطة |
| 12 | `/calendar` | ①③ | كما هو |
| 13 | `/operations` | ⑤⑥ | تحت «المزيد» (يُدمج في اليوم لاحقاً) |
| 14 | `/dashboard` | تحليل | تحت «المزيد» |
| 15 | `/reports` | ⑦ | تحت «المزيد» |
| 16 | `/accounting` | ⑦ | ✅ «الفلوس والمستحقات»: المتأخرات · باقي على العملاء · علينا للموردين |
| 17 | `/integrity` | تقني | تحت «المزيد» |
| 18 | `/search` | مساعد | تحت «المزيد» |
| 19 | `/settings` | إعداد | تحت «المزيد» |
| 20–21 | لينا · «اسمع الصفحة» | مساعد | كما هي |
| 22 | 10 مستندات طباعة | ②⑤⑦ | كما هي (أزرارها داخل التبويبات) |
| 23 | بصمة الوجه | ⑥ | ✅ الطريق الافتراضي: «بصمة التحرك» / «بصمة الانتهاء» |

---

## 3. مساحة عمل المناسبة — الشكل النهائي

```
[ملخص] [① العرض والعربون] [② المضيفون] [③ العدة ✓] [④ يوم المناسبة] [⑤ الإقفال] [السجل]
        └ التسعير · المدفوعات        └ المخزن · المواد · المشتريات   └ الحضور · الأجور   └ الفواتير · المالية
```

- الصف الأول شريط أفقي واحد بنفس الحجم؛ الصف الثاني أقسام المرحلة المختارة فقط.
- ✓ تُشتق من `event_command_center` (عربون مسجَّل؟ مضيفون مكتملون؟ عدة جاهزة؟ الكل بصم؟ الباقي صفر؟).
- `?tab=المعدات` القديم يتحوّل تلقائياً إلى `المخزن` (`normalizeWorkspaceTab`).
- الصلاحيات كما هي: التبويبات المالية لا تظهر لمن لا يملك `cost.visibility`.

---

## 4. ما نُفِّذ فعلاً (بالـ commits)

| commit | ماذا |
|---|---|
| `f63219f` | المراحل الخمس · مناسبة جديدة = السريعة · بصمة التحرك/الانتهاء · بطاقة الإقفال · هاتف واحد · مصطلحات |
| `fb990bd` | صفحة اليوم للموبايل · لغة بسيطة · قائمة «المزيد» مجموعتين |
| `09e05b7` | migration 0104 (دوال الأعمار volatile) · لغة الفلوس والمضيفين والدخول |
| `125bf32` `f0f2ae0` | شريط المراحل أفقي فوق · أرقام لاتينية في كل التواريخ · إزالة شريط البراندينج المعطوب · أيقونة الحالة الفارغة |
| `e322490` | **دمج المعدات في المخزن** · أيقونات القائمة الجانبية · اليوم عمودان على الديسكتوب |
| (هذه الجولة) | صفحة «المخزن»: العدة \| المواد — تبويب العدة يقرأ `equipment_capacity` + RPC `equipment_availability` (متاح/محجوز 7 أيام) |

---

## 5. ما لن نلمسه (لأنه صحيح)
- كل أوامر الخادم (RPC)، قاعدة البيانات، الصلاحيات، RLS، الـ idempotency، التدقيق.
- محرك بصمة الوجه، حساب الأجور، السلف والصرف، دفتر المخزن، الفواتير.
- المستندات المطبوعة العشرة، لينا، «اسمع الصفحة».

---

## 6. المتبقي — يحتاج قرار المالك أو تجربة فعلية

| # | البند | ملاحظة |
|---|---|---|
| 1 | **تطبيق migration 0104** على Supabase (`npx supabase db push`) | بدونها شاشة الفلوس تعرض خطأ |
| 2 | إرسال العرض للعميل عبر واتساب بضغطة | غير موجود؛ الموجود `omanWhatsAppUrl` للمضيفين فقط. يُبنى بعد أول مناسبة حقيقية |
| 3 | دمج `/operations` و`/dashboard` في «اليوم» نهائياً | بعد أسبوعين استخدام |
| 4 | «المطاعم المتعاقدة» | تُخفى إن لم تُستخدم |
| 5 | البيانات التجريبية | لا يوجد أمر مسح. الأسلم: منظمة جديدة نظيفة للمكتب |
| 6 | معاينة الطباعة (10 مستندات) على الموبايل | لم تُعاين بعد |

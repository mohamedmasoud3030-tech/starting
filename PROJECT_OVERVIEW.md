# PROJECT_OVERVIEW.md — نظرة شاملة موثّقة على المشروع

> وثيقة استكشافية واقعية أُعدّت في 2026-08-17 من فحص مباشر للمستودع والكود وقاعدة البيانات ونتائج التشغيل وCI.
> كل بند هنا مُصنَّف: ✅ مُتحقق منه، 🔶 استنتاج معقول، ❓ مجهول لم يتم التحقق منه.

---

## 1. ما هو المشروع؟

نظام ويب **عربي أولاً (RTL)** لإدارة عمليات الضيافة والمناسبات، مصمَّم لمكاتب خدمات
الضيافة في **سلطنة عُمان** (أعراس، عزاء، مجالس، اجتماعات، إفطار/غداء/عشاء، فعاليات).

الفكرة المحورية: **تحويل طلب العميل إلى مناسبة منفَّذة ومغلقة وربحية** — من تسجيل
الطلب، مروراً بعرض السعر والتأكيد والتحضير، وحتى الإغلاق وحساب الربح الفعلي.

- **اسم الحزمة:** `hospitality-ops` (الإصدار `0.1.0`) ✅
- **المستودع:** `mohamedmasoud3030-tech/starting` ✅
- **الفرع الحالي:** `arena/01a00fa3-starting` (محلي فقط — لم يُدفع بعد إلى GitHub) ✅
- **نقطة الأساس:** `main @ 375b31d` — آخر دمج هو PR #25 «Phase 3» ✅
- **الترخيص:** جميع الحقوق محفوظة (بدون ترخيص مفتوح) ✅

## 2. المستخدمون والأدوار

**المستخدم الأساسي:** مالك المكتب (50+ عاماً). البساطة متطلب منتج: خط كبير، أزرار
واضحة، أهداف لمس كبيرة، عربية أولاً.

الأدوار (enum `app_role` في قاعدة البيانات) ✅:

| الدور | المعنى | أهم الصلاحيات |
| --- | --- | --- |
| `OWNER` | المالك | كل شيء + إدارة العضويات وقراءة التدقيق |
| `MANAGER` | مدير | التهيئة التجارية (كتالوج/باقات) + كل العمليات |
| `SUPERVISOR` | مشرف | عمليات تشغيلية (طاقم/معدات/مخزن/عملاء) بدون أسعار التكلفة |
| `WAREHOUSE` | عامل مخزن | الصرف/الإرجاع الفعلي فقط، بدون أي بيانات مالية |
| `ACCOUNTANT` | محاسب | قراءة مالية (تكلفة/ملاحظات/مدفوعات) + كتابة مالية، بدون تهيئة تجارية |

فصل البيانات المالية **عند حدود قاعدة البيانات** (عبر RLS وread models)، وليس مجرد
إخفاء في الواجهة ✅. `cost_price` و`internal_notes` تُقرأ فقط من `OWNER`/`MANAGER`/`ACCOUNTANT`.

## 3. نموذج المستأجرين (Tenancy)

- منظمة واحدة = نشاط تجاري مستقل تماماً (Tenant). لا فروع ولا مواقع ✅.
- مستخدم واحد قد ينتمي لعدة منظمات بأدوار مختلفة؛ تبديل المنشأة = تبديل حساب ✅.
- العزل إلزامي عبر RLS + قيود مفاتيح أجنبية مركّبة تمنع عبور المنظمات بنيوياً ✅.
- منظمة غير نشطة تمنع القراءة والكتابة حتى مع عضوية نشطة ✅.

## 4. جرد المسارات (Routes) الكامل ✅

| المسار | الصفحة | ملاحظات |
| --- | --- | --- |
| `/` | إعادة توجيه إلى `/home` | — |
| `/login` | تسجيل الدخول | Supabase Auth (بريد + كلمة مرور) فقط؛ لا مسار تجريبي |
| `/home` | لوحة المتابعة التشغيلية | مناسبات اليوم + جاهزية + تنبيهات مخزون/حضور + واتساب + «اسمع الصفحة» |
| `/events` | قائمة المناسبات | إنشاء مناسبة جديدة + بطاقات بالحالة |
| `/events/$eventId` | مساحة عمل المناسبة | 11 تبويباً (انظر أدناه) |
| `/quotes` | عروض الأسعار | قائمة المسودات والعروض الصادرة |
| `/quotes/new` | عرض سعر جديد | معالج من 3 خطوات (تفاصيل ← خدمات ← مراجعة) |
| `/quotes/$quoteId` | تعديل/إصدار عرض سعر | مسودة قابلة للتعديل حتى الإصدار |
| `/customers` | العملاء | سجل عملاء المنظمة |
| `/catalog` | دليل الخدمات والمواد | أصناف + تصنيفات + تكلفة/بيع + طرق تسعير |
| `/packages` | الباقات | قوالب جاهزة قابلة لإعادة الاستخدام |
| `/consumables` | مخزون المواد | دفتر حركات المواد الاستهلاكية |
| `/procurement` | الموردون وأوامر الشراء | موردون + طلبات + استلام |
| `/staff` | المضيفون والأجور | طاقم + حضور + سلف/صرف + كشوف |

كل الصفحات محمية خلف `AuthGate`؛ الصفحات محمّلة كسولاً (lazy) عبر `routes.lazy.tsx` ✅.

### تبويبات مساحة عمل المناسبة ✅

`ملخص` (Overview) · `التسعير` (Pricing) · `الفريق` (Team) · `المعدات` (Equipment) ·
`المخزن` (Warehouse) · `المواد` (Consumables) · `التوريد` (Procurement) ·
`المدفوعات` (Payments) · `الفواتير` (Invoices) · `الحضور` (Attendance) · `السجل` (History).

تبويبا «المدفوعات» و«الفواتير» يظهران فقط لمن يملك صلاحية قراءة التكلفة ✅.

## 5. جرد الميزات حسب المجال

### المناسبات (Event lifecycle) ✅
حالات صريحة: `DRAFT → QUOTED → CONFIRMED → PREPARING → DISPATCHED → IN_PROGRESS →
RETURNING → CLOSED` مع `CANCELLED`. انتقالات بقواعد عمل خادمية قابلة للتدقيق.

### عروض الأسعار (Quotations) ✅
نموذج موحّد `quotations` + `quotation_lines` (بعد R11):
`DRAFT → ISSUED → ACCEPTED → CONVERTED` مع `SUPERSEDED`/`CANCELLED`.
أرقام `QT-...` عند الإصدار فقط، لقطات سعرية ثابتة، حفظ ذري للمسودة، إعادة حساب
خادمية للإجماليات، وتحويل ذري إلى مناسبة `CONFIRMED` مع منع التكرار.

### الكتالوج والباقات ✅
أصناف بـ 8 أنواع (`SERVICE`, `REUSABLE_EQUIPMENT`, `CONSUMABLE`, `STAFF`,
`CATERING`, `TRANSPORT`, `ADDON`, `OTHER`) و7 طرق تسعير (`FIXED`, `PER_EVENT`,
`PER_GUEST`, `PER_UNIT`, `PER_HOUR`, `PER_DAY`, `MANUAL`). تعطيل/تفعيل بدل حذف.
الباقات قوالب تُطبَّق على العرض/المناسبة كلقطات مملوكة.

### المخزن: معدات قابلة لإعادة الاستخدام (S4A) ✅
دفتر حركات append-only: صرف، إرجاع سليم/تالف/مفقود، حجز زمني عبر `equipment_capacity`،
تسوية نهائية بمصادقة OWNER/MANAGER، وتقييم تالف/مفقود بلقطة سعر الكتالوج.

### المواد الاستهلاكية (S4B) ✅
دفتر `consumable_movements` append-only مستقل: استلام، صرف، مرتجع، استهلاك، هالك،
تعديل موثق — رصيد المخزن وعُهدة المناسبة مشتقان وممنوعان من السالب transactionally.

### المشتريات (S5) ✅
موردون + أوامر شراء `PO-YYYY-NNNNN` بدورة حياة كاملة (مسودة ← اعتماد ← إرسال ←
تأكيد ← استلام جزئي/كامل ← إلغاء) ولقطات أسعار تفاوضية، وربط الاستلام بحركات S4B.

### المدفوعات والاقتصاديات (S6) ✅
سجل `customer_payments` إلحاقي (طرق: نقدي/تحويل/بطاقة/شيك/محفظة/أخرى) مع إلغاء غير
تدميري، وأساس الإيراد = العرض المعتمد، والتكلفة من ملخصات المشتريات.

### لوحة المتابعة (S7) ✅
مناسبات اليوم بتوقيت `Asia/Muscat`، جاهزية عبر `event_readiness`، تنبيهات مخزون
منخفض وفجوات حضور، مشاركة تذكير واتساب (بيانات تشغيلية فقط — بلا أسعار).

### الحضور والأجور والفواتير (S9) ✅
حضور/انصراف بورديتين، حساب ساعات وأجر (4 طرق احتساب)، سلف وصرف بأرشيف كامل،
فواتير رسمية بأقساط (عربون/أقساط/نهائي) مربوطة بالعرض والمناسبة، ومركز انتباه
المالك على اللوحة الرئيسية.

### وضع «اسمع الصفحة» (Owner Voice) ✅
راوٍ عربي حتمي (deterministic) مبني على بيانات الشاشة المنظمة — وليس قارئ شاشة
وليس ذكاءً اصطناعياً — عبر `speechSynthesis` بالمتصفح، مع زر واحد وسرعات 3،
وبلا أي إرسال لبيانات العمل إلى الخارج، ومحترم لنفس صلاحيات الدور (لا ينطق تكلفة
لمن لا يحق له رؤيتها).

### PWA ✅
Manifest عربي RTL + Service Worker بكاش ثابت فقط (script/style/font/image) + قشرة
تطبيق للفتح دون اتصال + عدم اعتراض أي طلبات API/Auth/طفرات. (التفاصيل في ARCHITECTURE.md)

## 6. ما هو خارج النطاق (مُعلن صراحةً)

لا ERP محاسبة كامل، لا نظام رواتب، لا CRM أتمتة، لا مساعد ذكاء اصطناعي، لا تسويق،
لا microservices، لا event sourcing. لا بوابة دفع، لا إرسال بريد، لا خرائط، لا
تحليلات، لا push notifications، ولا طابور مزامنة offline ✅ (من وثائق النطاق).

---

## 7. فهرس الوثائق الرسمية في المستودع

- `docs/architecture/01–20` — المنتج، دورة الحياة، النموذج التجاري، الأمن، المخزن،
  خارطة الطريق، المشتريات، المدفوعات، اللوحة، الحضور، الفواتير، R10/R11.
- `docs/operations/` — النشر، النسخ/الاستعادة، UAT، إغلاق S8/S9.
- `docs/refactor/` — تقارير مهمّات سابقة (baseline, phase 2, phase 3) وتدقيق قاعدة البيانات.
- `docs/ci/README.md` — عقد CI.
- `AGENTS.md` — قواعد هندسية إلزامية (نقود/تسعير/أمن/ترحيلات/UX/جودة).

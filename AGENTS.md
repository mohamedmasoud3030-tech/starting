# AGENTS.md — قواعد هندسية غير قابلة للتفاوض

هذا الملف هو المرجع الملزم لأي وكيل أو مطوّر يعمل على هذا المستودع. أي كود يخالف هذه
القواعد يُرفض في المراجعة.

## النقود (Money)

- العملة الأساسية هي **الريال العماني (OMR)** وبدقة **3 خانات عشرية** في كل الحفظ.
- التخزين في قاعدة البيانات يكون `numeric(12,3)` **حصراً** (الحد الأقصى `999,999,999.999`).
- **ممنوع** استخدام حساب الفاصلة العائمة الثنائية كمصدر حقيقة مالية:
  - لا `Number(...)` ولا `parseFloat(...)` ولا `price * quantity` كقيمة محفوظة نهائية.
- كل الحساب في الذاكرة يكون بأعداد صحيحة "ملي-ريال" (1 OMR = 1000) عبر
  [`src/lib/money.ts`](src/lib/money.ts)، والضرب يستخدم `BigInt` لتفادي تجاوز
  الدقة الوسيط.
- الواجهة تفرض **نفس نطاق** `numeric(12,3)` حتى لا تُرسل قيمة ترفضها قاعدة البيانات.
- قواعد التقريب **مركزية** في `src/lib/money.ts` (تقريب نصف بعيداً عن الصفر إلى 3 خانات).
- النقل من/إلى قاعدة البيانات يكون كنصوص عشرية دقيقة (PostgREST يعيد `numeric` كنص).

## التسعير (Pricing)

- **لا أسعار تجارية مكتوبة في الكود** كمصدر حقيقة. كل سعر قابل للتهيئة من المالك.
- **سعر التكلفة** و**سعر البيع** مفهومان منفصلان دائماً (لكل صنف ذي صلة).
- أسعار المناسبات تستخدم **لقطات سعرية (Snapshot)**، ولا ترتبط حياً بسعر الكتالوج.
- **الأحداث التاريخية لا يُعاد تسعيرها أبداً** بسبب تغيير سعر الكتالوج لاحقاً.

## المناسبة (Event)

- **المناسبة هي المجمّع التشغيلي المركزي** للنظام.
- الباقات هي **قوالب (Templates)**؛ أسطر المناسبة هي **لقطات خاصة بالمناسبة**.
- لا تبنِ باقة تجعل المناسبات التاريخية معتمدة على حالة القالب.

## المخزن (Inventory)

- **المعدات القابلة لإعادة الاستخدام** و**المواد الاستهلاكية** دلالتان مختلفتان تماماً،
  ولا تُنمذجان بنفس الطريقة.
- عمليات المخزن يجب أن تكون قابلة للتدقيق (auditable).
- التوفر الزمني (حجز عبر فترات مناسبات متداخلة) سيكون مطلوباً — لا تبنِ نموذج
  "رصيد ثابت" يمنع ذلك لاحقاً.

## الأمن (Security)

- **عزل المنظمات** إلزامي عبر RLS + قيود سلامة مركّبة حيث أمكن.
- **فحوصات الأدوار في الواجهة ليست تفويضاً.** التفويض يجب أن يُفرض في قاعدة البيانات.
- الكتابات الحساسة تستخدم حدود معاملات آمنة (دوال RPC) حيث تتعلق سلامة العمل.
- **ممنوع** وجود مسار دخول تجريبي (demo) أو بيانات دخول صلبة أو مستخدمين وهميين أو
  أي fallback صامت للأدوار.
- **فصل البيانات المالية عند حدود البيانات**: سعر التكلفة والملاحظات الداخلية
  تُقرأ فقط من أدوار `OWNER`/`MANAGER`/`ACCOUNTANT` (وليس مجرد إخفاء في الواجهة).
- **التدقيق إلحاقي وداخلي فقط**: لا مسار استدعاء عميل لـ `record_audit`؛ تُسجَّل
  أحداث التدقيق من الأوامر المعتمدة، وقراءتها محصورة بـ `OWNER`/`MANAGER`.
- **لا حذف تدميري للبيانات الرئيسية**: الكتالوج/التصنيفات/الباقات تستخدم
  `ACTIVE`/`INACTIVE`؛ لا سياسات `DELETE` من العميل.
- **المنظمة غير النشطة** تمنع القراءة والكتابة المحميتين حتى مع عضوية نشطة.

## الترحيلات (Migrations)

- الترحيلات المطبَّقة **خالدة** — لا تعدّل ملفاً طُبِّق سابقاً.
- أي تغيير جديد يكون **بترحيل جديد**.
- السلسلة يجب أن تعمل من قاعدة فارغة (`supabase db reset`).

## تجربة الاستخدام (UX)

- **العربية RTL أولاً** دائماً (`<html dir="rtl" lang="ar">`).
- **البساطة متطلب منتج** (المستخدم الأساسي مالك 50+ عاماً): خطوط كبيرة مقروءة،
  أزرار واضحة، أهداف لمس كبيرة، تنقل محدود.
- **لا وحدات وهمية** — لا أزرار بلا فعل، ولا صفحات نائبة تُقدَّم كمنجزة، ولا إحصائيات
  مختلقة.

## الجودة (Quality)

- إنتاجي فقط: لا بيانات إنتاج وهمية، لا أمان نائب، لا `any` منتشر.
- الثوابت الهامة **تتطلب اختبارات** (نقود، تسعير، عزل، لقطات).
- `npm run typecheck` و`npm run lint` و`npm test` و`npm run build` يجب أن تنجح جميعها.

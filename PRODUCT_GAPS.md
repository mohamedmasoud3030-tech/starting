# PRODUCT_GAPS.md — ما الذي ينقص المنتج وما نُنفَّذ (تقييم تجربة حقيقية)

> أُعدت بعد تنفيذ رحلات المستخدم كاملة ضد قاعدة بيانات حية (39/39 خطوة ناجحة)
> وبعد مراجعة تجربة الانطلاق الفعلية. المالك غير تقني — هذا الملف بلغة بسيطة.

## ١. ما أُنجز في هذه الجولة (كان عائق الانطلاق الأول)
- **تسجيل حساب جديد** (`/signup`) — أول خطوة لأي مستخدم.
- **إنشاء المنشأة ذاتياً** بعد أول دخول (الترحيل 0061) — لم يعد المستخدم الجديد
  عالقاً في حلقة دخول/خروج؛ يصبح مالك منشأته فوراً.
- **استعادة كلمة المرور** (`/forgot-password`) — الحل القياسي لنسيان كلمة المرور.

## ٢. المزايا الناقصة الموصى بها (بالأولوية)
1. **إدارة أعضاء الفريق** — مكتب الضيافة الحقيقي يضم مشرفاً ومحاسباً وعمال مخزن.
   اليوم لا توجد شاشة يضيف فيها المالك مستخدميه بأدوارهم. التوصية: شاشة «فريق
   المنشأة» في إعدادات المالك (إضافة بالبريد الإلكتروني + دور + إيقاف).
2. **تقارير الربح الدورية** — وعد المنتج «مناسبة مغلقة وربحية» يحتاج شاشة تقارير
   شهرية/فصلية (إجمالي الإيراد، التكلفة، الهامش) بدل النظر لكل مناسبة على حدة.
3. **طباعة الفواتير وعروض الأسعار (PDF)** — العميل يحتاج ورقة رسمية؛ حل الطباعة
   من المتصفح (تنسيق طباعة عربي) هو الأسرع والأقل كلفة.
4. **كشف حساب العميل** — صفحة واحدة لكل عميل: مناسباته، عروضه، مدفوعاته، متبقيه.
5. **عرض أسبوعي في اللوحة** — اليوم تعرض اللوحة مناسبات اليوم فقط؛ عرض الأسبوع
   يساعد التخطيط المسبق.
6. **تصدير CSV** للقوائم (مناسبات، مدفوعات، حضور) لفتحها في Excel.
7. **اختبار أجهزة حقيقية** — تثبيت PWA وجودة الصوت العربي على هواتف فعلية.
8. **مراقبة الأعطال** (إرسال أخطاء الواجهة لمكان واحد) واختبارات متصفح آلية.
9. **موجة تحديث أدوات البناء** (Vite 8) — مؤجلة كموجة واحدة (لا قيمة عاجلة).

## ٣. خطوة الإنتاج المطلوبة بعد كل دمج
كل ترحيل جديد (مثل 0061) يُطبَّق على قاعدة الإنتاج بالأمر الموثق في
`OPERATIONS.md §9`: `supabase db push` بعد نسخة احتياطية. بدونها تظهر الأزرار
الجديدة في الموقع المنشور لكن الخادم يرفض تنفيذها.

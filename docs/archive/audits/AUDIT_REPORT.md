# AUDIT_REPORT.md — المراجعة الوظيفية والتشغيلية للنظام مقابل النشاط الحقيقي

> أُعدّت 2026-08-18 من فحص الكود والمخطط والتدفقات الفعلية (وليس من وعود
> تسويقية). المرجع التجاري: `BRAND_IDENTITY.md` (مشاريع جودة الإنطلاقة — قسم
> خدمات الضيافة وتنظيم الحفلات).
>
> حالة كل عنصر: **موجود وجيد** · **موجود ويحتاج تحسين** · **ناقص وضروري** ·
> **ناقص ويمكن تأجيله** · **غير مطلوب** · **✅ منفَّذ** (بعد بدء التنفيذ).

---

## ملاحظة اختبار (تصحيح دقيق — لا تسمية مضللة)

- **pgTAP** = اختبارات تكامل على مستوى قاعدة البيانات (invariants + سلوك RPC/RLS).
- **Vitest (jsdom + Testing Library)** = اختبارات وحدة + مكوّنات.
- **`smoke:production`** = برهان بناء إنتاجي (مسارات HTTP، CSP، PWA، حدود chunk) — ليس E2E لمسار العمل.
- **لا يوجد harness E2E على مستوى المتصفح/التطبيق** (لا Playwright ولا Cypress؛
  `@vitest/browser-playwright` تبعية اختيارية عابرة غير مفعّلة، وvitest مضبوط على
  jsdom فقط). مسار Phase B التجاري مغطى بـ **pgTAP + اختبارات المكوّنات**، وليس
  بـ browser E2E. إضافة harness متصفح حقيقي قرار لاحق منفصل (وليس إلزامياً الآن
  لعدم وجود أداة متصفح في هذه البيئة لتشغيله والتحقق منه).

## ⏳ حالة التنفيذ (متتبَّع — يُحدَّث مع كل مرحلة)

### Phase A — Foundation: ✅ منفَّذة بالكامل

| البند | قبل | بعد |
| --- | --- | --- |
| إعدادات وهويمة المنشأة (Gap #16) | ناقص وضروري | ✅ منفَّذ — ترحيل `0061_organization_settings` + شاشة `/settings` |
| طباعة/PDF المستندات (Gap #15) | ناقص وضروري | ✅ منفَّذ — Document Engine + معاينة/طباعة في عرض السعر |
| هوية المنشأة في البيانات | `organizations.name` فقط | ✅ 22 حقلاً هوية/تواصل/قانوني/ترقيم/توقيع في `organization_settings` |
| إعدادات النظام كفئة | غائبة | ✅ جدول إعدادات منفصل عن بيانات النشاط + أمر OWNER-only مُدقَّق |
| ترقيم المستندات | بادئات صلبة `EV/QT/PO` | ✅ بادئات قابلة للتهيئة (تُستهلك لاحقاً عند توليد الأرقام) |
| المنطقة الزمنية | `Asia/Muscat` افتراضي | ✅ مُهيّأة في المنظمة + تُعرض الوثائق بتوقيت مسقط |

**الأدلة:** `supabase/migrations/20260818160000_0061_organization_settings.sql`
+ `supabase/tests/organization_settings.test.sql` (12 تأكيداً). بوابات الجودة
كلها خضراء (typecheck 0 · lint 0/0 · test 552/74 · build · smoke · pgTAP 17 ملفاً).

### Phase B — Commercial: ✅ منفَّذة

| البند | قبل | بعد |
| --- | --- | --- |
| ترقيم المستندات (AC #1) | بادئات صلبة EV/QT/PO | ✅ `next_document_number` يشتق البادئة من `organization_settings` (ذري/concurrency-safe/scoped) |
| مكونات السعر (AC #2) | `total_selling` = مجموع الأسطر فقط | ✅ `subtotal + transport + surcharge - discount = grand total` مع `quotation_pricing()` كمصدر حقيقة واحد |
| الخصم (AC #2) | غير موجود | ✅ نوع `FIXED`/`PERCENT` + تحقق (نسبة ≤100، إجمالي غير سالب، خصم غير سالب) |
| الباقة كلقطة (AC #3) | موجود | ✅ مثبت باختبار انحدار (تعديل الباقة لا يغيّر اللقطة) |
| نطاق الضيوف (AC #4) | `base_guest_count` فقط | ✅ `min_guests`/`max_guests` كتوصية (لا قيد صارم) |
| دورة حياة العرض (AC #5) | DRAFT→ISSUED→ACCEPTED→CONVERTED | ✅ + REJECTED/EXPIRED/CANCELLED/SUPERSEDED مع آلة حالات صريحة ومدققة |
| الصلاحية (AC #6) | غير موجودة | ✅ `valid_until` + `is_expired` مشتق (بدون cron) |
| النسخ (AC #7) | `revision` عدّاد بلا آلية | ✅ `revise_quotation`: نسخة جديدة تشترك بالرقم، القديم SUPERSEDED لا يُمحى |
| القبول الذري (AC #8) | موجود (idempotent) | ✅ محفوظ + انتقالات محمية من القبول بعد الرفض |
| التحويل (AC #9) | موجود (idempotent/atomic) | ✅ مثبت: تحويل مكرر لا ينشئ مناسبة ثانية + فصل snapshot/خطة |
| النقل (AC #10) | صنف `TRANSPORT` يدوي | ✅ `transport_required` + مبلغ + نطاق/ملاحظة (بلا خوارزمية/سعر افتراضي) |
| واجهة التحرير (AC #11) | خطوات متفرقة | ✅ خطوة تسعير موحّدة (نقل/رسوم/خصم/صلاحية) + ملخص فرعي/نهائي |
| المستند (AC #12) | بنود + إجمالي | ✅ يعرض subtotal/نقل/رسوم/خصم/إجمالي/صلاحية/نسخة |
| حماية البيانات (AC #13) | — | ✅ أعمدة nullable/افتراضي + backfill `subtotal=total_selling` بلا قيم مختلقة |
| الثوابت (AC #14) | — | ✅ 22 تأكيد pgTAP (أرقام، لقطات، خصم، سالب، انتقالات، تحويل، نسخ، عزل) |

**الأدلة:** ترحيلات `0062`–`0065` + `supabase/tests/commercial_engine.test.sql`
(22 تأكيداً). بوابات الجودة كلها خضراء (typecheck 0 · lint 0/0 · test 558/75 ·
build · smoke · pgTAP 18 ملفاً · types drift مطابق).

### Phase C — Operations: ✅ منفَّذة

| المطلب | الحالة |
| --- | --- |
| A. Event Workspace كمركز قيادة | ✅ Overview يجمع: التفاصيل + العرض المعتمد/النسخة + الجاهزية + الخط الزمني + الإجراءات |
| B. Readiness Engine قابل للتفسير | ✅ `readinessReport.ts` (rule-based) + لوحة تعرض النسبة + قائمة ✓/✕ «مطلوب/مخصص» |
| C. المطلوب مقابل المخصص | ✅ لكل مورد (فريق ككل + معدات لكل صنف) — البنود غير المطبقة تُستبعد من المقام |
| D. التعارضات | ✅ القاعدة تبقى السلطة (`STAFF_CONFLICT`/`EQUIPMENT_SHORTAGE` عبر exclusion/locks)؛ الواجهة تعرض النقص قبل الحفظ |
| E. Calendar | ✅ `/calendar` (شهر/يوم) بقراءة readiness مجمّعة |
| F. Operations Board | ✅ `/operations` (اليوم/غداً/غير جاهزة/بانتظار الإرسال/الإرجاع) |
| G. انتقالات مع override | ✅ ترحيل `0066`: بوابة جاهزية على `PREPARING→DISPATCHED` + تجاوز صريح مُدقَّق (سبب ≥3 أحرف + جدول إلحاقي + audit) |
| H. Mobile-first | ✅ إجراءات تشغيلية بعناوين لمس كبيرة + تقويم/لوحة تشغيل متجاوبان |

**قرارات معمارية:** آلة الحالات **لم تتغيّر** (كما وافق المالك)؛ `READY` مشتقة لا حالة؛
الإغلاق المالي سيُبنى في Phase D كأمر صريح مدقق (ليس Boolean مشتقاً).

**الأدلة:** ترحيل `0066` + `supabase/tests/event_transition_override.test.sql` (8 تأكيدات)
+ `readinessReport.test.ts` (6) + `ReadinessReportPanel.test.tsx` (3) + `calendarModel.test.ts` (3).
بوابات الجودة كلها خضراء (typecheck 0 · lint 0/0 · test 570/78 · build · smoke ·
pgTAP 19 ملفاً · types drift مطابق).

### Phase D — Finance: ✅ منفَّذة

| المطلب | الحالة |
| --- | --- |
| D1. Unified Expense Model | ✅ `expense_category` enum + `event_expenses` (append-only, VOID، OMR دقيق، idempotent) |
| D2. لا تضاعف المشتريات | ✅ المشتريات من procurement وحدها؛ STAFF/SUPERVISION من payroll؛ الباقي من expenses — كل مصدر يُعدّ مرة واحدة |
| D3. Staff Cost | ✅ تكلفة العمالة الفعلية من `earned_total` (أجر مسجَّل فعلاً، لا افتراضي) |
| D4. Collections/Receivables | ✅ Revenue (معتمد) ≠ Collected ≠ Outstanding، منفصلة في العرض والقراءة |
| D5/D6. Actual Profitability + breakdown | ✅ `event_finance_summaries` أعيد بناؤها: staff_cost + procurement_cost + expense_cost → actual_cost/actual_profit/margin_percent + توزيع تكلفة لكل فئة |
| D7. Financial Close Snapshot | ✅ `event_financial_closures` يخزّن revenue/collected/outstanding/costs/profit/margin وقت الإغلاق |
| D8. منع تعديل مالي بعد الإغلاق | ✅ حارس triggers على customer_payments/host_payouts/staff_attendance/event_expenses |
| D9. Event Workspace financial section | ✅ تبويب «المالية»: القيمة/المحصل/المتبقي/المصروف/الربح/الهامش + المصروفات + الإغلاق المالي |
| D10. Financial Integrity tests | ✅ 20 تأكيد pgTAP (عزل، سالب، لا تضاعف، close غير جاهز، double close، تعديل بعد close، reopen، تاريخ لا يُمحى) |

**قرار معماري (كما وافق المالك):** الإغلاق المالي **حدث أعمال** في جدول تاريخي إلحاقي
`event_financial_closures` (دورة OPEN → CLOSED → REOPENED → CLOSED AGAIN)، مستقل عن
`events.status`. الحالة المالية مشتقة (نشط = صف بلا `reopened_at`) لا Boolean. أمران
صريحان `close_event_financially` / `reopen_event_financially` ذريان مُدقَّقان،
وإعادة الفتح لا تمحو تاريخ الإغلاق السابق.

**الأدلة:** ترحيلات `0067`–`0069` + `supabase/tests/financial_closures.test.sql`
(20 تأكيداً) + `finance.api.test.ts` (3) + تحديث `payments.api.test.ts`.
بوابات الجودة كلها خضراء (typecheck 0 · lint 0/0 · test 573/79 · build · smoke ·
pgTAP 20 ملفاً · types drift مطابق).

### Phase E — Intelligence: ✅ منفَّذة

| المطلب | الحالة |
| --- | --- |
| E0/E3. Smart Alerts Engine | ✅ `management_alerts()` — مصدر تنبيهات واحد (server-side)، severity INFO/WARNING/CRITICAL، قابل للتفسير والتوجيه |
| E1. Management Dashboard | ✅ `/dashboard` — تشغيل/مبيعات/مالية من `management_metrics()` مع فصل صريح Revenue/Collected/Profit |
| E2. Today Command Center | ✅ قسم «ما يحتاج انتباهي» من نفس دالة التنبيهات |
| E4. Customer 360 | ✅ `customer_360()` + صفحة `/customers/$customerId` (حقائق مفهومة، بلا score مزيّف) |
| E5. Global Search | ✅ `global_search()` (bounded LIMIT، org-scoped) + `/search` |
| E6. Integrity Center | ✅ `integrity_findings()` + `/integrity` (كشف فقط، لا إصلاح تلقائي) |
| E7. Reports | ✅ `report_events/customers/packages` + `/reports` (استخدام مقابل ربحية للباقات) |
| E8. Time filters | ✅ `rangeForFilter` (حدود يوم Muscat) |
| E9. Drill-down | ✅ كل KPI/tanbih Link إلى السجل المصدر |
| E12. Performance | ✅ لا N+1: دالتا SQL مجمعتان + batch readiness مُعاد استخدامه |
| E13. Permission | ✅ لوحة الإدارة/التقارير/مركز السلامة معروضة لـ cost roles فقط (financial nav flag)؛ التنبيهات المالية داخل الدالة مشروطة بـ can_read_cost |

**الأدلة:** ترحيلات `0070`–`0072` + `supabase/tests/intelligence.test.sql` (15 تأكيداً:
عزل منظمات، فصل المالية، بحث محدود/معزول، سلامة بيانات، تقارير) + اختبارات واجهة.
بوابات الجودة كلها خضراء (typecheck 0 · lint 0/0 · test 578/81 · build · smoke ·
pgTAP 21 ملفاً · types drift مطابق).

## 🏁 الجاهزية النهائية (Phase E20 — مراجعة شاملة A–E)

راجع `FINAL_READINESS_REPORT.md` للتقرير الكامل.

---

## 0. الخلاصة التنفيذية

---

## 0. الخلاصة التنفيذية

النظام **مكتمل هندسياً وأمنياً** (كل البوابات خضراء، 36 جدول أعمال، RLS على
الكل، نقود OMR بدقة 3 خانات، لقطات أسعار تاريخية) و**يغطي دورة العمل الحقيقية
بشكل شبه كامل** على مستوى البيانات والتدفقات.

لكن **غير جاهز للاستخدام الفعلي في المكتب بعد** لثلاث فجوات جوهرية واحدة منها
حرجة:

1. **لا توجد طباعة/تصدير للمستندات إطلاقاً** — عروض الأسعار والفواتير تُعرض
   على الشاشة فقط، بينما نشاطك مبني على **ورقة رسمية مختومة تصل للعميل**. هذه
   الفجوة الحرجة.
2. **هوية المنشأة غير ممثلة في قاعدة البيانات** — جدول المنظمة يحمل الاسم فقط
   (بلا شعار، هواتف، س.ت، عنوان، ألوان). فلا يمكن لمستند أن يحمل هوية
   «مشاريع جودة الإنطلاقة».
3. **المصروفات والربحية الفعلية جزئية** — تكلفة المشتريات تُحسب، لكن أجور
   المضيفين والنقل والمصروفات الأخرى لا تدخل في رقم «الربح الفعلي» الموحّد.

دورة العمل كاملة موجودة وظيفياً: عميل ← مناسبة ← عرض سعر ← اعتماد ← حجز ←
تجهيز ← تنفيذ ← تحصيل ← ربحية. النموذج قادر على استيعاب التدفق؛ الفجوات في
«التقديم للمستند» و«الهوية» و«توحيد التكلفة» لا في بنية التدفق.

---

## 1. ما الموجود حالياً ويخدم دورة العمل الحقيقية

| الجزء | الحالة | الدليل |
| --- | --- | --- |
| العملاء | موجود وجيد | `customers`: اسم، جوال، واتساب، نوع (فرد/شركة/جهة حكومية)، ملاحظات، نشط/غير نشط |
| المناسبات والحجوزات | موجود وجيد | دورة حياة كاملة `DRAFT→…→CLOSED` + `CANCELLED`، رقم مناسبة `EV-…`، موقع، عدد ضيوف، سجل تحولات |
| عروض الأسعار | موجود وجيد | دورة `DRAFT→ISSUED→ACCEPTED→CONVERTED`، لقطات (snapshot) غير قابلة للتغيير بعد الإصدار، أرقام `QT-…` |
| الباقات (قوالب) | موجود وجيد | `packages` + `package_items` كقوالب، تطبيقها ينسخ **لقطات مملوكة** لا ترتبط حياً بالقالب |
| الإضافات/التعديلات | موجود وجيد | سطر `is_custom` في أسطر العرض والمناسبة؛ إضافة بنود يدوية فوق الباقة |
| عدد الضيوف | موجود وجيد | `guest_count` على المناسبة + لقطة على العرض + تسعير `PER_GUEST` |
| المضيفون/المشرفون | موجود وجيد | `staff_type` يشمل `HOST/HOSTESS/SUPERVISOR`؛ راتب لكل مناسبة/ساعة/يوم/يدوي |
| تخصيص الموظفين | موجود وجيد | `event_staff_assignments` مع رفض تداخل الأوقات |
| المعدات وأدوات الضيافة | موجود وجيد | `equipment_capacity` + حجوزات زمنية + دفتر إرسال/إرجاع (سليم/تالف/مفقود) + تسوية نهائية |
| المخزون/المستهلكات | موجود وجيد | دفتر كميات إلحاقي (استلام/صرف/مرتجع/استهلاك/هالك/تسوية) بلا أرصدة سالبة |
| العربون والتحصيلات | موجود وجيد | دفتر `customer_payments` + فواتير بأقساط `DEPOSIT/INSTALLMENT/FINAL`؛ المدفوع/المتبقي مشتق |
| ربحية المناسبة (إجمالي) | موجود وجيد جزئياً | عرض `event_finance_summaries`: إيراد، ربح متوقع، مدفوع، متبقي، تكلفة ملتزمة/مستلمة، هامش إجمالي |

**الاستنتاج:** النواة التشغيلية والمالية كاملة تقريباً. العروض الأربعة القديمة
(140/230/310/400) يمكن نمذجتها اليوم كـ **4 باقات قابلة للتعديل** + أصناف
كتالوج (مضيفون، دلال، فناجين، مستلزمات) — بلا أي تغيير كود.

---

## 2. ما الموجود لكنه مصمم بطريقة لا تناسب نشاطنا

| العنصر | المشكلة | الأثر | القرار المقترح |
| --- | --- | --- | --- |
| **رسوم النقل** | لا يوجد حقل/مفهوم «نقل»؛ يوجد فقط نوع صنف `TRANSPORT` يُضاف يدوياً | قاعدة «داخل نزوى فقط، وخارجها يُضاف نقل» تُطبَّق ذهنياً كل مرة، عرضة للنسيان | إضافة مفهوم نقل (نطاق/رسوم حسب الموقع) لاحقاً — انظر P1 |
| **الخصم** | لا تمثيل للخصم إطلاقاً؛ كل الأسطر `quantity > 0` و`total ≥ 0` | أي تخفيض على العرض يتطلب تعديل يدوي للأسعار، بلا أثر تدقيقي | آلية خصم واضحة — انظر P1 |
| **نطاق الباقات (المعازيم)** | `base_guest_count` رقم واحد؛ لا يوجد «من 10 إلى 100» كحدّ | اختيار الباقة المناسبة يدوي، بلا منع وضع باقة 100–200 لمناسبة 50 شخصاً | اقتراح/تحقق من النطاق — انظر P2 |
| **الربحية الفعلية** | الهامش يحسب تكلفة المشتريات فقط؛ أجور المضيفين والنقل ومصاريف أخرى **لا تدخل** | «الربح الفعلي» الظاهر أقل من الحقيقة، قرارات التسعير قد تكون مضللة | توحيد المصروفات — انظر P1 |
| **المنطقة الزمنية للهاتف** (D17) | حقل التاريخ يستخدم توقيت جهاز المستخدم لا توقيت مسقط | خطأ محتمل في مواعيد المناسبات على جهاز بتوقيت آخر | تثبيت توقيت مسقط — انظر P1 |

---

## 3. ما الناقص فعلاً (مؤكد من الكود)

| العنصر | الخطورة | التفصيل |
| --- | --- | --- |
| **طباعة/PDF المستندات** | **حرجة** | لا يوجد أي `window.print` أو تصدير PDF في المشروع كله (`grep` عبر `src/`). عروض الأسعار والفواتير شاشات فقط |
| **هوية المنشأة في البيانات** | عالية | `organizations` يحوي: اسم، اسم عرض، عملة، توقيت، نشط. **لا** شعار ولا هواتف ولا س.ت ولا عنوان ولا ألوان |
| **شاشة إعدادات المنشأة** | عالية | لا توجد أي واجهة لتعديل هوية المنشأة أو إعداداتها |
| **إعدادات النظام** | متوسطة | لا يوجد مفهوم «إعدادات» منفصل؛ كل شيء قيم افتراضية صلبة (`OMR`, `Asia/Muscat`) |
| **توحيد المصروفات الفعلية** | عالية | أجور العاملين (دفتر مستقل)، النقل، ومصاريف أخرى غير مجمّعة في رقم واحد للمصروفات |

---

## 4. ما الموجود ولا نحتاجه / زائد عن النشاط

النظام منضبط النطاق؛ **لا توجد ميزة كبيرة زائدة**. ملاحظات ثانوية فقط:

| العنصر | الملاحظة | القرار |
| --- | --- | --- |
| 8 أنواع أصناف + 7 طرق تسعير | نشاطك يحتاج نحو نصفها (خدمة، معدات، مواد، طاقم، نقل، إضافة) | إبقاؤها — مرونة غير مكلفة، لا تُعطّل شيئاً |
| نوع عميل «جهة حكومية» | غير مستخدم اليوم غالباً | إبقاؤه — لا ضرر |
| تبديل المنظمات (متعدد المكاتب) | أنت مكتب واحد اليوم | إبقاؤه — أساس عزل البيانات وأمانها، ويفيد لاحقاً |

> **تنبيه توثيقي** (لا كود): `DATA_MODEL.md §5` ما زال يذكر دور العرض العام
> `public_demo_admin` رغم أن الترحيل 0059 أزال وضع العرض نهائياً. انحراف وثائقي
> لا يؤثر على التشغيل، يُصحَّح لاحقاً.

---

## 5. هل نموذج البيانات قادر على استيعاب التدفق؟

**التدفق:** عميل → مناسبة → عرض سعر/باقة → موافقة → حجز → تجهيز موارد → تنفيذ
→ تحصيل → مصروفات → ربحية

| خطوة التدفق | القدرة | التقييم |
| --- | --- | --- |
| عميل | `customers` | ✅ كامل |
| مناسبة | `events` بدورة حياة كاملة | ✅ كامل |
| عرض سعر/باقة | `quotations` + `packages` + تطبيق باقة كلقطات | ✅ كامل (نطاق الباقة يدوي) |
| موافقة | `accept_quotation` (أمر خادمي مع تدقيق) | ✅ كامل |
| حجز | التحويل إلى مناسبة `CONFIRMED` | ✅ كامل |
| تجهيز موارد | حجز معدات + صرف مواد + تخصيص طاقم | ✅ كامل |
| تنفيذ | حالات `DISPATCHED→IN_PROGRESS→RETURNING` + حضور | ✅ كامل |
| تحصيل | دفتر مدفوعات + فواتير أقساط | ✅ كامل |
| مصروفات | مشتريات كاملة؛ أجور/نقل **غير مجمّعة** | ⚠️ جزئي |
| ربحية | `event_finance_summaries` (إيراد/هامش) | ⚠️ جزئي (الهامش دون الأجور/النقل) |

**الحكم:** النموذج **قادر** على استيعاب التدفق بالكامل. الفجوتان (مصروفات
موحّدة، ربحية فعلية) قابعتان للمعالجة **داخل البنية الحالية** بلا إعادة تصميم.

---

## 6. الفصل بين بيانات المنشأة / إعدادات النظام / بيانات النشاط

| الفئة | الوضع | التقييم |
| --- | --- | --- |
| **بيانات النشاط التشغيلي** (أحداث، عروض، مخزن…) | معزولة تماماً بـ `organization_id` مركّب في كل جدول | ✅ فصل ممتاز |
| **بيانات المنشأة** (الهوية) | حقلان فقط: `name`, `display_name` — بلا شعار/اتصال/س.ت | ⚠️ ناقصة النمذجة |
| **إعدادات النظام** | **لا يوجد جدول أو مفهوم إعدادات** — قيم افتراضية صلبة | ❌ غائب كفئة |

**المطلوب هيكلياً (لاحقاً):** توسيع `organizations` بحقول هوية (شعار، هواتف،
س.ت، رمز بريدي، عنوان، ألوان) — أو جدول `organization_settings` منفصل — ثم
شاشة إعدادات للمالك. يبقى الفصل سليماً: الهوية في المنظمة، والإعدادات منفصلة،
والنشاط التشغيلي معزول كما هو.

---

## 7. Gap Matrix الكاملة

| # | الجزء | الحالة | ملاحظة موجزة |
| --- | --- | --- | --- |
| 1 | العملاء | 🟢 موجود وجيد | اسم/جوال/واتساب/نوع/ملاحظات/نشط |
| 2 | المناسبات والحجوزات | 🟢 موجود وجيد | دورة حياة كاملة + رقم + سجل |
| 3 | عروض الأسعار | 🟢 موجود وجيد | لقطات + أرقام + اعتماد/تحويل |
| 4 | الباقات والقوالب واللقطات | 🟢 موجود وجيد | قوالب تُطبَّق كلقطات مملوكة |
| 5 | الإضافات والتعديلات | 🟡 موجود ويحتاج تحسين | أسطر يدوية موجودة؛ **لا خصم** |
| 6 | عدد الضيوف | 🟢 موجود وجيد | على المناسبة + لقطة + `PER_GUEST` |
| 7 | الموقع ورسوم النقل | 🟡 موجود ويحتاج تحسين | موقع نصي موجود؛ النقل يدوي بلا قاعدة |
| 8 | المضيفون والمشرفون | 🟢 موجود وجيد | أنواع طاقم + راتب لكل طريقة |
| 9 | تخصيص الموظفين | 🟢 موجود وجيد | مع رفض تداخل الأوقات |
| 10 | المعدات وأدوات الضيافة | 🟢 موجود وجيد | حجز/إرسال/إرجاع/تسوية |
| 11 | المخزون/المستهلكات | 🟢 موجود وجيد | دفتر كميات بلا سالب |
| 12 | العربون والتحصيلات والمتبقي | 🟢 موجود وجيد | مدفوعات + أقساط + مشتق المتبقي |
| 13 | المصروفات | 🟡 موجود ويحتاج تحسين | مشتريات كاملة؛ أجور/نقل غير مجمّعة |
| 14 | ربحية كل فعالية | 🟡 موجود ويحتاج تحسين | هامش إجمالي دون الأجور/النقل |
| 15 | الفواتير والطباعة | 🔴 ناقص وضروري | فواتير بيانات ✅ لكن **لا طباعة/PDF** |
| 16 | إعدادات وهوية المنشأة | 🔴 ناقص وضروري | لا شعار/اتصال/س.ت ولا شاشة إعدادات |
| 17 | ترقيم الصفحات للقوائم (D21) | 🟠 ناقص ويمكن تأجيله | تحذير حد العرض موجود |
| 18 | الحفظ التلقائي للمسودة (D22) | 🟠 ناقص ويمكن تأجيله | مانع فقدان التعديل موجود |
| 19 | توقيع رقمي/ختم | 🟠 ناقص ويمكن تأجيله | ضمن المستندات لاحقاً |
| 20 | تذكيرات/إشعارات عملاء | ⚪ غير مطلوب | عمداً خارج النطاق (`PRODUCT_SPEC`) |

---

## 8. أقصر مسار إلى نسخة إنتاج صالحة للاستخدام الفعلي

### المرحلة P0 — إلزامية قبل الإطلاق (بدونها لا يمكن للمكتب استخدام النظام فعلياً)

1. **طباعة وتصدير المستندات** — عروض الأسعار والفواتير كمستند قابل للطباعة/PDF
   بترويسة رسمية (الرسالة الفورية الأولى؛ نشاطك مبني على الورقة المختومة).
2. **هوية المنشأة + شاشة الإعدادات** — إضافة حقول هوية (شعار، هواتف، س.ت،
   رمز بريدي، عنوان، ألوان) وشاشة للمالك؛ تظهر تلقائياً في المستندات.
3. **تجهيز حساب الإنتاج والبيانات الافتتاحية** — إنشاء المنظمة الحقيقية وحساب
   المالك، وإدخال الكتالوج والباقات الأربع **كبيانات قابلة للتعديل** (وليس
   ثوابت كود). **يتطلب قرارات مالك** (حسابات Supabase/Vercel، نطاق، خطط).
4. **معاينة المستخدم النهائي (UAT)** — تجربة المالك للرحلة الكاملة مرة واحدة
   على بيانات حقيقية صغيرة.

### المرحلة P1 — بعد الإطلاق مباشرة (تحسينات تكمل الصورة المالية الصحيحة)

5. **توحيد المصروفات والربحية الفعلية** — ضمّ أجور العاملين والنقل والمصاريف
   الأخرى إلى رقم مصروفات/ربح واحد لكل مناسبة.
6. **مفهوم رسوم النقل** — حقل/بند نقل صريح مرتبط بالموقع (داخل/خارج نزوى).
7. **آلية الخصم** — تخفيض موثّق على العرض/الفاتورة بدل التعديل اليدوي.
8. **تثبيت توقيت مسقط** لإدخال المواعيد (D17).

### المرحلة P2 — لاحقاً (تحسينات جودة حياة، غير معطّلة)

9. نطاق الباقات (من/إلى معازيم) مع اقتراح الباقة المناسبة.
10. ترقيم الصفحات الكامل للقوائم (D21) والحفظ التلقائي (D22).
11. تواقيع رقمية/ختم، قوالب شروط، تصدير تقارير، عنوان العميل الكامل.

---

## 9. ما يتطلب قرارك قبل أي تنفيذ (لن أبدأ دونه)

1. **طباعة المستندات وهوية المنشأة** — أوسّع جدول المنظمة بحقول الهوية وأضيف
   شاشة إعدادات وطباعة؟ (تغيير مخطط جديد + شاشة جديدة)
2. **حسابات الإنتاج** — توفير حساب Supabase إنتاجي + نطاق Vercel (أسرار تحكمها
   أنت) لتجهيز بيئة المكتب الحقيقية.
3. **البيانات الافتتاحية** — اعتماد إدخال الباقات الأربع كبيانات (مع الإبقاء
   على تداخل الحدود كما هو دون «تصحيح»).

---

*انتهى التقرير. لم يُنفَّذ أي تغيير. بانتظار قرارك حول الأولويات وقبول/رفض
بنود P0 قبل بدء التنفيذ.*
